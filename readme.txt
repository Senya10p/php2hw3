PHP-2. Урок 3.
Домашняя работа.

1. Повторили код, изученный на уроке.
Выделили ту часть, которая управляет установкой и чтением произвольных свойств объекта в трейт (файл MagicTrait.php).

2. Добавили к своим данным таблицу авторы (authors). В таблице новостей добавили поле author_id.
1). Если запрашивается поле author, то сначала проверяется поле author_id.
2). Если оно не пустое, то делаем запрос к таблице authors. Возвращается результат в виде объекта класса Author.
3). Снабдили модели PHPDoc.

3. Изменили шаблоны приложения. Добавили вывод авторов новостей.

4. Изучили интерфейсы Countable (счётный) и Iterator (перебор). Реализовали их в классе View.
Iterator выделили в трейт (файл IteratorTrait.php).